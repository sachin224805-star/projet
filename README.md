Weconnect Java backend

Run with:

1. Set environment variables:
- MONGODB_URI (optional)
- JWT_SECRET (optional)

2. Build & run:

```bash
mvn package
java -jar target/weconnect-backend-java-0.0.1-SNAPSHOT.jar
```

API mirrors the Node backend: /api/auth and /api/reports
